#include <iostream>
using namespace::std;
float moduluss(int nilai,int n){
    if
        (n==0)
        {return (nilai%n);}
    else if
        (n==nilai)
        {return (0);}
    else
        {return (nilai%n);}
    }


main(){
    float nilai1,nilai2;
    cout<<"Masukan bilangan ke 1 : ";
    cin>>nilai1;
    cout<<"Masukan bilangan ke 2 : ";
    cin>>nilai2;
    cout<<"Hasil Modulus : ";
    cout<<moduluss(nilai1,nilai2);
    }
